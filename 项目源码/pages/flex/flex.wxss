/* pages/flex/flex.wxss */
.content{
  width: 100%;
  height: 100rpx;
  background-color: yellow;
  /* padding: 20rpx; */
  /* 
    弹性盒布局：提供了一种更灵活的布局模型
    让外部容器可以通过改变宽高、
    先后顺序对可用空间实现一些填充和布局的效果，
    更加方便了不同大小的内容区域布局适配
   */
  display: flex;
  /**
  row:从左到右的水平方向为主轴(默认取值)
  row-reverse:从右到左的水平方向为主轴
  column:从上到下的垂直方向为主轴
  column-reverse:从下到上的垂直方向为主轴
  */
  flex-direction: row;
  /* center:项目位于容器的中心 
     space-between:项目位于各行之间留有空白的容器内
     space-around:项目位于各行之前、之间、之后都留有空白的容器内
  */
  justify-content: space-around;
  /* 
    border-box：固定宽高之后，再去设置border和padding
    会从已知的宽高中减去对应border和padding的值
   */
  box-sizing: border-box;
  padding: 20rpx;
}